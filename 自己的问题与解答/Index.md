# 线性回归
- [线性回归到逻辑回归](线性回归到逻辑回归.md)
  - Q:为什么要在线性回归的基础上提出逻辑回归？相应的学习模型应该发生哪些变化？
  - Q:为什么交叉熵损失作为损失函数有效？是通过最大似然导出的吗？
- [自己实现和 torch 的 SGD 在调用反向传播的差别](<自己实现和 torch 的 SGD 在调用反向传播的差别.md>)
  - Q:为什么使用 torch 的高级 api 时，使用 l.mean() 进行反向传播，而自己实现时用 l.sum() 进行反向传播？

# Softmax回归
- [softmax的交叉熵损失函数](softmax的交叉熵损失函数.md)
  - Q:在分类问题的softmax回归中，交叉熵似然函数是怎么得到的？
  - Q:为什么这个多项分布的条件概率是这样的？
  - Q:我无法理解这里条件概率的含义
- [损失函数与似然函数](损失函数与似然函数.md)
  - Q:什么是损失函数的似然函数？
  - Q:似然函数是什么？
  - Q:知道损失函数可以反推似然函数吗？
- [样本不平衡带来的softmax回归问题](样本不平衡带来的softmax回归问题.md)
  - Q:如果样本不平衡，使用softmax回归会有什么问题？
- [torch自带的交叉熵损失函数解决指数爆炸问题](torch自带的交叉熵损失函数解决指数爆炸问题.md)
  - Q:pytorch 中的CrossEntropyLoss 函数是如何解决 softmax 中的指数爆炸问题的？
  - Q:如果使用 pytorch 中的 CrossEntropyLoss，最终所得模型输出不是规范的概率输出？

# 多层感知机
- [逻辑回归与单层感知机都是线性的](逻辑回归与单层感知机都是线性的.md)
  - Q:机器学习二分类问题中，logistic 回归和感知机的区别，在于感知机输出分类而 logistc 输出概率，而且感知机只能处理线性问题，而 logistic 可以处理非线性问题？
  - Q:softmax 也是无法处理非线性边界的吗？
  - Q:多层感知机能处理非线性问题吗？
- [多层神经网络的梯度下降和torch自动求导](多层神经网络的梯度下降和torch自动求导.md)
  - Q:多层的神经网络是如何进行梯度下降的？
  - Q:torch的自动求导是怎么处理这样的情况的（不用给出完整代码，解释原理）

# 过拟合和欠拟合
- [模型容量与迭代过拟合区别](模型容量与迭代过拟合区别.md)
  - Q:模型容量过大带来的过拟合和训练迭代次数过大带来的过拟合有什么区别？

# 数值稳定性和模型初始化
- [前向传播、反向传播和计算图（自动求导原理）](前向传播、反向传播和计算图（自动求导原理）.md)
  - 本部分直接来自于课本。用向量求导的数学工具，更好地理解反向传播以及梯度爆炸和梯度消失的原理。
- [Xavier初始化的原则背后的原理](Xavier初始化的原则背后的原理.md)
  - Q:为了避免较深的多层感知机在梯度下降过程中发生梯度爆炸和梯度消失，在模型初始化时采用Xavier方法。为什么让每层的输出和权重保持期望为零、方差恒定就可以减小梯度失效的概率？
  - Q:为什么能够假设每层的权重独立同分布，且输入和权重独立？
  - 具体的计算过程参考[让训练更加稳定.pdf](../ppt/让训练更加稳定.pdf)
- [16位浮点数破坏数值稳定性的解决方案](16位浮点数破坏数值稳定性的解决方案.md)
  - Q:在深度学习中使用16位浮点数虽然获得了效率提升，但对训练时的数值稳定性影响很大（梯度爆炸和梯度消失）。一般是如何处理这个问题的？

# 实战Kaggle——房价预测
- [kaggle-house-price.ipynb](../实战Kaggle教程——房价预测/kaggle-house-price.ipynb)
  - 直接从 d2l 仓库复制的记事本文件。展示了如何参加一次 Kaggle 竞赛的详细过程。
  - 需要注意的新点：**数据预处理、对数方均根误差的定义和使用、K折交叉验证的实现**。

